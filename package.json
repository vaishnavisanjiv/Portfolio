{
  "name": "portfolio",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist",  // Add this line to define the deploy script
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "@vitejs/plugin-react": "^3.1.0"
    // Add any other dependencies your project requires
  },
  "devDependencies": {
    "vite": "^4.1.0",
    "gh-pages": "^5.0.0"
    // Add any other dev dependencies your project requires
  },
  "homepage": "https://vaishnavisanjiv.github.io/Portfolio/"  // Adjust to your GitHub Pages URL
}


